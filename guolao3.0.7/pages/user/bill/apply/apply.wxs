//脚本 大类length == 1前+'0' 保留两位
var getNum = function (num) {
	if (num) {
		if (num.length == 1) {
			return ('0' + num)
		} else {
			return num;
		}
	}
};


var toFixed = function (num) {
	return parseInt(num).toFixed(2);
};

module.exports = {
	getNum: getNum,
	toFixed: toFixed,
};